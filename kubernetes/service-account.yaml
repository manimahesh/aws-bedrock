# service-account.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  # 1. Choose a meaningful name for your Service Account
  name: travel-agent-sa
  # 2. Specify the Kubernetes namespace your application will run in (e.g., 'default')
  namespace: default
  # 3. This is the IRSA Annotation that links this SA to your IAM Role
  annotations:
    # 🚨 REPLACE the ARN below with the full ARN of your EKS-TravelAgent-BedrockRole 
    eks.amazonaws.com/role-arn: arn:aws:iam::623386377217:role/eksctl-mmani-bedrock-cluster-addon-iamservice-Role1-bPCxOVfJlmfc